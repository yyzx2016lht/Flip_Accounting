# 手势后台保活机制说明

本文记录敲击手势服务的保活写法，目标是让手势检测在应用退到后台或服务被系统回收后尽量恢复，同时不申请 `POST_NOTIFICATIONS` 通知权限。

## 核心原则

1. 前台服务仍然需要创建通知和通知渠道，但 manifest 不声明 `android.permission.POST_NOTIFICATIONS`。
2. 复活条件只看真实检测开关：`Prefs.isDoubleTapEnabled(ctx)`。
3. 不要用页面级或入口级总开关阻断服务恢复，例如不要让 `quick_gesture_enabled` 卡住 `BootReceiver`、`onCreate()` 或 `onStartCommand(null)`。
4. 服务被移除或销毁时，用 `AlarmManager.setExact(...)` 做一次短延迟重拉保险。
5. 检测器在息屏时可以停止，亮屏/解锁时再恢复，避免无意义耗电。

## Manifest

保留前台服务、开机广播、悬浮窗、传感器和唤醒相关权限：

```xml
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
```

不要声明：

```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

## BootReceiver

`BootReceiver` 只判断敲击是否启用。满足条件时启动 `OverlayService`，让服务自己从 `Prefs` 恢复检测器。

```kotlin
val tapEnabled = Prefs.isDoubleTapEnabled(context)
if (!tapEnabled) return

val serviceIntent = Intent(context, OverlayService::class.java)
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
    context.startForegroundService(serviceIntent)
} else {
    context.startService(serviceIntent)
}
```

不要依赖 `quick_gesture_enabled` 之类 UI 总开关，否则会出现单项检测开着但服务无法自启的情况。

## OverlayService 恢复

`onCreate()` 和 `onStartCommand(null)` 都应该直接读取检测开关：

```kotlin
isDoubleTapEnabled = Prefs.isDoubleTapEnabled(this)
if (isDoubleTapEnabled) startTapDetection()
```

`ACTION_START_DOUBLE_TAP`、`ACTION_STOP_DOUBLE_TAP` 只负责用户主动切换时的即时启动/停止。系统恢复服务时不一定带 action，所以 `null` 分支必须能独立恢复。

## 被杀后的重拉

在 `onDestroy()` 和 `onTaskRemoved()` 里，只要任一检测开关仍然开启，就安排一次重启：

```kotlin
if (Prefs.isDoubleTapEnabled(this)) {
    scheduleRestart(RESTART_DELAY_MS)
}
```

`scheduleRestart()` 使用 `setExact(...)`：

```kotlin
val triggerAt = System.currentTimeMillis() + delayMs
am.setExact(AlarmManager.RTC_WAKEUP, triggerAt, pi)
```

这比普通 `set(...)` 更接近旧版硬重拉行为。`setExact` 在 Doze 中仍可能被系统延迟，但屏幕亮起后通常会恢复。

## 通知渠道

不申请通知权限并不代表服务不需要通知对象。前台服务仍需：

```kotlin
startForeground(NOTIF_ID, buildNotification("记账助手正在后台运行"))
```

通知渠道可以保持最低存在感：

```kotlin
NotificationChannel(CHANNEL_ID, "记账助手服务", NotificationManager.IMPORTANCE_MIN).apply {
    setShowBadge(false)
    enableLights(false)
    enableVibration(false)
    setSound(null, null)
}
```

`NotificationCompat.Builder` 也应设置为静默、最低优先级：

```kotlin
.setPriority(NotificationCompat.PRIORITY_MIN)
.setSilent(true)
.setOngoing(true)
.setShowWhen(false)
.setOnlyAlertOnce(true)
```

## 不推荐默认强 WakeLock

`PARTIAL_WAKE_LOCK` 能提高存活率，但会明显增加耗电。默认不要开启长期强 WakeLock。只保留短时 WakeLock，用于启动检测器、弹窗或 watchdog 重建时争取几秒 CPU 时间：

```kotlin
keepAliveManager.acquireWakeLockBriefly(3_000L)
```

如果未来要恢复强 WakeLock，应该作为明确的高级选项，并在 UI 中标注耗电风险。
