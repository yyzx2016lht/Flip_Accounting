# FlipAccounting-AI Bug 排查清单（完整版）

> 按模块分类，逐项排查。每项完成后打 `[x]`，发现问题记录在条目下方。
> 版本：深度代码扫描后更新，覆盖全部已实现功能。

---

## 🧭 通用 / 跨模块

### 页面导航与手势
- `[ ]` 四个主 Tab（账单、统计、资产、设置）**左右滑动切换**，动画是否流畅，无卡顿/白屏/闪烁
- `[ ]` 滑动到一半放手后**回弹动画**是否正确还原（不误切换到目标 Tab）
- `[ ]` 在统计页 PieChart 区域**左右滑**，是否正确让 PieChart 优先处理手势（而非切页）
- `[ ]` 账单首页左边缘向右滑，是否打开**账本侧边栏 Drawer**，不误触切换到上一 Tab
- `[ ]` 底部 Tab 点击切换时，**FAB 加号按钮**只在账单 Tab 显示，切换到其他 Tab 后自动隐藏
- `[ ]` 资产 Tab 的**资产专属 FAB**，在 Tab 切换动画期间是否正确隐藏/显示（无闪烁）
- `[ ]` **资产功能关闭**（设置开关关闭后），底部 Nav 中资产 Tab 是否消失，滑动手势是否跳过该 Tab

### 年月选择器（YearMonthPicker）
- `[ ]` 年月选择器只出现在应当出现的页面（账单首页、统计页），**不应出现**在分类管理、备份、次级账单等页面
- `[ ]` 切换年月后，账单列表和统计数据是否立即刷新
- `[ ]` `SharedYearMonthSession` 在 Activity 重建（旋转屏幕）后是否正确恢复

### 多账本
- `[ ]` 切换账本后，账单列表、统计、资产是否全部切换到正确账本的数据
- `[ ]` 删除账本时，「仅删除账本」和「删除账本及所有账单」两种模式数据清理是否正确
- `[ ]` 账本侧边栏 Drawer 打开状态下横屏旋转，是否崩溃或 Drawer 状态异常

### 数据库 & 数据一致性
- `[ ]` 数据库升级（`MigrationManager.kt`）：升级后旧数据完整，无字段丢失
- `[ ]` 并发写入保护：连续快速点击「保存账单」是否产生重复条目
- `[ ]` `normalizedTimeMillis`：时间戳有些是秒级有些是毫秒级，账单时间显示是否全部正确（无 1970 年异常）

### 货币 & 汇率
- `[ ]` 多货币账单在首页、统计页的金额换算是否正确
- `[ ]` 汇率为 0 或未设置时，是否有保护逻辑，不出现除零崩溃
- `[ ]` 货币符号与负数金额的组合显示是否正确（`-¥100` vs `¥-100`）
- `[ ]` 多货币功能关闭时，汇率管理菜单入口是否隐藏

---

## 📋 账单模块（Home / 账单）

### 账单列表
- `[ ]` 账单列表按日期分组，跨月/跨年边界排序正确
- `[ ]` 账单列表为空时，显示空状态占位，不显示空白页
- `[ ]` 大量数据（>1000 条）滚动是否流畅（`homeRecycledViewPool` 缓存池有效）
- `[ ]` 长按多选模式：选中态高亮正确，批量删除后列表立即刷新
- `[ ]` 多选模式下，切换到其他 Tab 再回来，多选状态是否正确清除

### 添加 / 编辑账单（EditBillActivity）
- `[ ]` 金额键盘：输入超长金额（如 9999999999.99）是否溢出或崩溃
- `[ ]` 金额为 0 时，是否阻止保存并给出提示
- `[ ]` 分类选择后返回，分类显示是否正确更新
- `[ ]` 日期时间选择器正常工作，极端日期（1970-01-01、2099-12-31）不崩溃
- `[ ]` **退款功能**：退款账单与原账单关联正确，退款后资产余额联动更新
- `[ ]` 编辑已有账单：修改金额后，**关联资产账户余额**是否同步更新（差额处理）
- `[ ]` 账单备注超长文本（200+ 字符），是否有截断，不破坏列表布局
- `[ ]` **多账单模式**（`isMultiBillEnabled`）：一次性创建多条账单后，列表是否正确显示全部

### 账单搜索（BillSearchActivity）
- `[ ]` 搜索结果实时响应，去抖动无明显延迟
- `[ ]` 清空关键词后，是否恢复全部列表
- `[ ]` 搜索结果点击跳转到账单详情

### 日历视图（CalendarActivity）
- `[ ]` 日历上每天金额与账单列表一致
- `[ ]` 点击某天，底部列表正确筛选该天账单
- `[ ]` 月份切换时，日历数据重新加载
- `[ ]` 无账单的日期不显示金额（不显示 ¥0）

### 首页图表 & 趋势卡片（HomeChartController）
- `[ ]` 趋势卡片开关（设置 → 显示趋势卡）关闭后，首页趋势卡消失；重新开启后出现
- `[ ]` 图表数据为空（当月无账单）时，图表区域优雅降级，不崩溃
- `[ ]` 图表点击某天，账单列表是否正确滚动/高亮到该天

---

## 📊 统计模块（Stats）

### 概览卡片
- `[ ]` 总收入、总支出、结余金额计算正确，与账单列表总额一致
- `[ ]` 筛选（收入/支出/全部）切换后，图表和列表同步更新

### 图表
- `[ ]` 图表在数据为空时（当月无账单）不崩溃，优雅降级
- `[ ]` 图表 X 轴日期标签与实际数据对齐
- `[ ]` 月度 vs 年度视图切换：数据聚合方式正确
- `[ ]` 图表点击某条柱/点，底部详情列表正确筛选

### 分类统计
- `[ ]` 饼图/分类列表的百分比加总约为 100%（浮点精度问题）
- `[ ]` 点击某分类，跳转查看该分类下的所有账单
- `[ ]` 分类顺序按金额从大到小排列
- `[ ]` 筛选条件（账本、日期范围、收支类型）组合使用结果正确

### 统计筛选（StatsPopups / layout_stats_filter_sheet）
- `[ ]` 筛选弹窗关闭后，已选条件正确显示在主页标题区
- `[ ]` 重置筛选条件后，统计数据恢复为默认
- `[ ]` 跨账本统计（「全部账本」模式），数据合并计算正确
- `[ ]` 图表设置（`dialog_chart_settings`）：修改图表类型/颜色后立即生效

---

## 💰 资产模块（Assets）

### 资产列表（AssetsFragment）
- `[ ]` 总资产金额 = 各资产余额之和，计算正确（含负债账户）
- `[ ]` 资产功能关闭再开启后，资产列表数据完整
- `[ ]` 资产列表为空时，显示空状态引导

### 资产详情（AssetDetailActivity）
- `[ ]` 详情页账单列表只显示该资产关联的账单
- `[ ]` **余额调整**（BalanceAdjustmentActivity）：调整后资产余额立即更新，调整记录在账单列表中显示
- `[ ]` 收入/支出统计与账单列表数据吻合

### 添加 / 编辑资产（AddAssetActivity）
- `[ ]` 资产名称为空时，阻止保存
- `[ ]` 初始余额为负数（负债账户）允许输入并正确保存
- `[ ]` 资产图标选择后，正确保存并显示
- `[ ]` 编辑资产修改货币类型，关联账单的金额处理逻辑正确

### 资产统计（AssetStatsActivity）
- `[ ]` 资产历史余额折线图与实际账单流水一致
- `[ ]` 日期区间选择后，图表和明细正确刷新
- `[ ]` 图表日期 Chip 选择（`item_asset_stats_date_chip`）交互正常

---

## ⚙️ 设置模块（Profile 页 + 各 Settings Activity）

### 个人资料
- `[ ]` 用户名称/头像/描述修改后，设置页立即刷新显示
- `[ ]` 头像裁剪（UCrop）：选图→裁剪→保存流程正常，文件存储在 `filesDir/chat_user_avatar.jpg`
- `[ ]` 头像图片文件不存在时（手动删除），显示默认头像，不崩溃

### 分类管理（SettingsActivity）
- `[ ]` 收入/支出 Tab 切换后，分类列表正确重载
- `[ ]` 一级分类展开/收起子分类面板，指示箭头/选中态正确
- `[ ]` **新增分类**：名称为空、超长时有校验
- `[ ]` **删除分类 → 有关联账单 → 迁移**：账单被正确迁移到目标分类
- `[ ]` **删除分类 → 有关联账单 → 连同删除**：账单全部删除，无孤儿数据
- `[ ]` **删除分类 → 无关联账单**：直接删除，无确认弹窗卡死
- `[ ]` **排序模式**：拖拽排序后点保存，顺序持久化；点取消，顺序不变
- `[ ]` **子分类升级为一级分类**（promoteToParent）：升级后显示在正确位置
- `[ ]` **一级分类降级为子分类**（demoteToChild）：降级后出现在目标一级分类的展开面板中
- `[ ]` 一级分类有子分类时，阻止直接删除，提示先处理子分类

### 账本管理（BookOverviewActivity）
- `[ ]` 新增账本：同名账本被拦截（重复检测）
- `[ ]` 账本概览卡片金额/账单数量与实际数据一致
- `[ ]` 删除账本两种模式（仅删除/连同删除）数据处理正确

### 备份与恢复（BackupActivity）
- `[ ]` 本地备份：备份文件生成后，路径提示正确，文件实际存在
- `[ ]` 恢复备份：数据完整还原，恢复后无缺失账单/分类/资产
- `[ ]` **部分恢复**（`dialog_restore_modules`）：只恢复账单/只恢复资产等场景按预期工作
- `[ ]` 云备份（CloudBackupActivity）：网络异常时友好提示，不崩溃
- `[ ]` 恢复后 App 是否需要重启，是否有引导

### 显示设置（BillDisplaySettingsActivity）
- `[ ]` 开启/关闭「分类图标」「完整分类名」「备注优先」「金额分组」，账单列表立即更新
- `[ ]` 设置项重启后持久化（`Prefs` 读写正确）

### AI 功能开关 & 入口
- `[ ]` 关闭「AI 功能」开关后，AI 相关菜单项隐藏；重新开启后恢复
- `[ ]` FAB 加号按钮：「传统记账模式」vs「AI 对话模式」两种模式切换是否正确（`Prefs.getAiEntryMode`）

### AI 配置（AiConfigActivity / AiFeatureSettingsActivity）
- `[ ]` API Key 为空时，使用 AI 功能给出友好提示而非崩溃
- `[ ]` **多模型分配**（单条/多条/规则/小票/截图/语音各用不同模型）：各场景是否真正使用对应模型
- `[ ]` **自定义提示词**（AiPromptEditorActivity）：保存后影响 AI 记账结果
- `[ ]` **AI 规则**（AiRuleManageActivity）：规则按预期触发，关闭后不触发
- `[ ]` **本地规则覆盖**（`isLocalRuleOverrideEnabled`）：开启后 AI 规则优先级高于 AI 返回

### AI 对话（ChatActivity）
- `[ ]` 发送文字后，AI 正常回复，账单卡片正确显示金额/分类/备注
- `[ ]` 账单卡片点击「保存」：账单写入数据库，回到首页账单列表更新
- `[ ]` 账单卡片点击「修改」：弹出修改界面，修改后保存正确
- `[ ]` **语音输入**（ChatVoiceInputController）：录音→识别→填入输入框流程正常
- `[ ]` **图片上传识别**（ChatMediaController）：选图→压缩→发送→AI 识别，结果正确
- `[ ]` **对话会话管理**（ChatSessionController）：新建会话、切换会话、删除会话正常
- `[ ]` 对话历史搜索（ChatSearchActivity）：搜索结果准确
- `[ ]` 长对话（100+ 条消息）滚动性能是否流畅
- `[ ]` AI 连续回复中途断网，界面是否优雅提示错误，不卡死

### 翻转手势 & 悬浮窗（FlipDetector / OverlayService / OverlayManager）
- `[ ]` 翻转触发后，悬浮窗正确弹出记账界面
- `[ ]` 翻转灵敏度调节（FlipSensitivityActivity）：调整后实际触发阈值符合设置
- `[ ]` 悬浮窗权限未授予时，开启翻转功能后，提示引导用户去系统设置
- `[ ]` 悬浮窗拖动到屏幕边缘，不超出屏幕范围
- `[ ]` **横屏禁用翻转**（`isFlipDisableLandscape`）：横屏时翻转无效，竖屏恢复正常
- `[ ]` **震动反馈**（`isVibrateFeedbackEnabled`）：翻转触发时震动，关闭设置后无震动
- `[ ]` 悬浮窗关闭后，资源正确释放（无内存泄漏/前台服务残留）

### Shizuku 模式 & 白名单（ShizukuSafe / AppListActivity）
- `[ ]` Shizuku 未启动时，开启白名单模式，给出友好提示（`showShizukuPrompt`）
- `[ ]` 白名单模式开启后，只在白名单 App 前台时触发翻转
- `[ ]` 关闭 Shizuku 模式后，白名单开关自动重置为关闭
- `[ ]` **截屏记账**（ScreenCaptureActivity）：需要 Shizuku 授权，未授权时提示，授权后截图识别正常

### 存储管理（StorageCleanupActivity）
- `[ ]` 存储用量统计正确（图片/语音/日志各分类占用）
- `[ ]` 清理操作执行后，文件实际删除，占用数字更新
- `[ ]` 清理 AI 对话图片后，对话历史中的图片显示是否出现异常（文件已删除但 UI 未处理）

### 日志（LogViewerActivity）
- `[ ]` 开启日志后，操作产生日志；关闭后，日志停止写入
- `[ ]` 分享日志文件功能正常工作

---

## 🔧 系统级 & 边缘场景

### 权限
- `[ ]` 首次安装，通知权限（Android 13+）引导申请
- `[ ]` 拒绝权限后，相关功能优雅降级，不崩溃

### 生命周期
- `[ ]` 旋转屏幕（横竖屏切换），各页面不崩溃、数据不丢失
- `[ ]` App 进入后台 10 分钟再切回，数据仍然正确显示
- `[ ]` 快速连续点击按钮，防止多次跳转或重复操作（防抖）

### 性能
- `[ ]` 账单数据量大（>1000 条）时，首页列表滚动流畅（`homeRecycledViewPool` 发挥作用）
- `[ ]` 统计页大量数据聚合在后台线程执行，不阻塞 UI（不出现 ANR）
- `[ ]` 内存泄漏：`ProfileFragment` 的 `appbarLayoutListener` 在 `onDestroyView` 中是否正确清理（代码已有，验证有效）

### 极端数据
- `[ ]` 金额极大值（如 ¥99,999,999.99）：显示是否溢出 UI 布局
- `[ ]` 备注/分类名超长文本：有截断，不破坏列表布局
- `[ ]` 账本名称为空或超长，是否有校验
- `[ ]` 时间戳为 0 或负数的历史账单，显示是否正常

### 开机自启（BootReceiver）
- `[ ]` 手机重启后，若翻转手势已开启，`BootReceiver` 是否自动拉起 `OverlayService`

---

## 📝 排查记录

在此记录发现的 Bug：

| # | 模块 | 描述 | 严重程度 | 状态 |
|---|------|------|----------|------|
| 1 | | | | |
| 2 | | | | |

> 严重程度：🔴 崩溃 / 🟠 功能异常 / 🟡 显示问题 / 🟢 体验优化
