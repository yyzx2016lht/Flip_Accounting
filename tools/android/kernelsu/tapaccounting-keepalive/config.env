# Package and entry points.
PKG=com.taostudio.tapaccounting
USER_ID=0
RESTART_ACTION=com.taostudio.tapaccounting.RESTART_SERVICE

# Poll interval in seconds. Lower = faster recovery after force-stop.
CHECK_INTERVAL=10

# Log path.
LOG=/data/adb/tapaccounting-keepalive.log
