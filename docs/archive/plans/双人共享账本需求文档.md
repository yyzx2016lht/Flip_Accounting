# FlipAccounting 双人共享账本需求文档

## 一、产品目标

为 FlipAccounting 增加"双人共享账本"功能，适用于情侣、夫妻等两个互相信任的人共同记账。

两个人分别在自己的 Android 手机上使用应用，通过坚果云 WebDAV 同步同一个账本。

### 核心要求

1. 两个人都能看到共享账本中的全部账单
2. 两个人都能新增账单，但只能编辑和删除自己创建的账单
3. 每笔账单都能看出由谁创建（即由谁付款）
4. 两个人的支出共同消耗共享账本预算
5. 每个人的银行卡、现金、支付宝等资产仍然只保存在自己的手机上
6. 离线时可以正常记账，恢复网络后自动同步
7. 同步时合并双方的操作，不能用云端文件整体覆盖本地数据库
8. 重复同步、网络中断、应用退出或操作乱序时，不能产生重复账单、重复扣款或已删除账单复活

**首版只面向两个互相信任的成员，不建设服务器和账号系统。**

---

## 二、用户角色

每个共享账本固定包含两个成员：

- 成员 1
- 成员 2

### 成员身份

- 每名成员拥有永久且唯一的成员身份（UUID）
- 成员身份不能使用昵称，因为昵称允许为空、重复和修改
- 成员名称为空时，界面显示"成员1""成员2"
- 成员名称修改后，历史账单的成员归属不能发生变化

### 权限

两个人权限相同：
- 都可以新增账单
- 都可以修改共享预算
- 但**只能修改和删除自己创建的账单**，无法编辑对方的账单

---

## 三、共享账本与个人账本

应用必须同时支持：
- 普通个人账本
- 双人共享账本

个人账本继续按照现有方式工作，不参与共享同步。

### 共享账本身份

每个共享账本拥有独立且永久的共享身份（UUID）。共享账本名称只是显示信息，不作为同步身份。

修改共享账本名称不能：
- 创建一个新共享账本
- 丢失成员信息
- 丢失待同步操作
- 丢失删除记录
- 影响历史账单归属

### 数据分类

**共享账本中的业务数据（需要同步）：**
- 账单
- 账单删除状态
- 预算定义
- 成员信息
- 必要的同步版本信息

**本地私有数据（不同步）：**
- 本机资产账户
- 本机资产余额
- 账单与本机资产的绑定
- WebDAV 密码
- 本机同步队列和同步运行状态

---

## 四、分类处理

### 设计原则

- **不共享分类**：两台设备的分类列表各自独立
- **账单中存储分类信息**：账单中直接存储分类名称和图标URL
- **对方能看到账单分类**：但对方的分类列表中不会出现这个分类
- **统计只统计自己**：统计页面只统计自己创建的账单

### 实现方式

账单中直接存储：
- `cateName`：分类名称（如"餐饮-外卖"）
- `cateIcon`：分类图标URL

对方收到账单后：
- 能看到"这个账单是什么分类"
- 但不会在自己的分类列表中添加这个分类
- 统计时不统计对方的账单

---

## 五、创建共享账本

用户可以通过以下方式创建共享账本：

### 创建流程

1. 用户在账本管理页面选择"设定为共享账本"
2. 输入账本名称（默认为当前账本名称）
3. 输入两名成员的显示名称（允许留空）
4. 配置坚果云 WebDAV（地址、用户名、应用密码）
5. 应用创建共享账本和远端共享目录
6. 应用生成另一名成员加入所需的信息（JSON格式，可分享）

### 加入信息格式

```json
{
  "ledgerId": "uuid",
  "ledgerName": "共享账本",
  "webdavUrl": "https://dav.jianguoyun.com/dav/",
  "webdavUser": "user@example.com",
  "remotePath": "/shared-ledger/uuid/",
  "memberId": "member-uuid-for-joining",
  "memberName": "成员2",
  "protocolVersion": 1
}
```

- 编码为 Base64 字符串
- 可以通过文本分享或二维码分享
- **不包含 WebDAV 密码**（密码由用户手动输入）

---

## 六、加入共享账本

第二名成员需要通过加入流程连接同一个共享账本。

### 加入流程

1. 用户输入或粘贴加入信息
2. 输入 WebDAV 应用密码
3. 应用验证远端共享账本存在
4. 应用验证共享账本身份一致
5. 应用验证成员数量为两人
6. 确认当前手机对应哪名成员
7. 在本机创建或选择一个本地账本承载共享数据
8. 下载并应用全部历史共享操作
9. 显示首次同步进度和错误

### WebDAV 密码安全

WebDAV 密码不能：
- 写入共享账本文件
- 写入二维码
- 写入普通导出文件
- 写入日志
- 写入明文数据库字段
- 进入 `.bak` 备份
- 提交到 Git

密码应保存在 Android 提供的安全存储中。

---

## 七、共享账单

### 7.1 新增账单

任意成员都可以在共享账本中新建账单。

新建账单时需要记录：
- 跨设备唯一的账单身份（UUID）
- 所属共享账本
- 创建成员（即付款成员）
- 金额、类型、分类、时间、备注
- 币种及现有账单所需业务字段
- 是否排除统计
- 当前版本
- 创建时间和操作时间

**账单必须先保存在本地，然后再同步。**

即使当前没有网络：
- 账单也必须保存成功
- 界面应立即显示账单
- 本地资产应按照规则立即更新
- 账单显示"待同步"状态
- 恢复网络后自动上传

不能因为上传失败而撤销用户已经保存的账单。

### 7.2 修改账单

**只有创建者可以修改自己创建的账单。**

修改后：
- 账单版本增加
- 修改先在本地生效
- 生成一条待同步的修改记录
- 另一台设备联网后接收修改

### 7.3 删除账单

**只有创建者可以删除自己创建的账单。**

删除后：
- 本机账单立即从正常列表移除
- 生成永久的删除同步记录
- 不能删除坚果云上的历史操作文件
- 另一台设备收到删除记录后删除对应本地账单
- 更早的修改记录以后再次出现时，不能让账单复活

如果删除的是当前成员支付的账单，应在付款人的设备上恢复对应资产；该恢复最多执行一次。

### 7.4 退款

退款应继续遵循应用现有业务规则。

退款账单必须通过跨设备账单身份关联原账单，不能使用本地数据库数字 ID 作为共享关联。

退款对预算和资产的影响应与现有个人账单语义保持一致。

### 7.5 暂不支持的账单类型

首版同步协议暂时不支持某些特殊账单：
- 转账
- 还款
- 余额调整

这些类型的账单在共享账本中**仅本地展示**：
- 影响本地资产
- 影响本地预算
- 显示在共享账本的账单列表中
- 但**不会上传到云端进行同步**
- 对方看不到这些账单

---

## 八、成员归属展示

### 账单列表

共享账单列表需要展示创建成员（即付款成员）。

**位置**：账单列表项中，资产账户的左侧

```
┌─────────────────────────────────────────────────────────────┐
│  [图标]  餐饮-外卖                                  -100.00 │
│                          [成员1]  现金                      │
└─────────────────────────────────────────────────────────────┘
```

- 成员名称显示在资产账户左侧
- 成员名称支持用户更改（但只修改显示名称，不影响成员身份）
- 如果成员名称为空，显示"成员1""成员2"

### 账单详情

账单详情页显示创建成员（即付款成员）。

**说明**：只显示一个成员信息即可，因为：
- 只有创建者可以编辑自己的账单
- 创建成员 = 付款成员 = 最后修改成员

---

## 九、共享预算

预算属于共享账本，不属于某一个成员。

### 预算消耗

例如，共享账本的伙食预算为 1500 元：
- 成员 1 支出 100 元
- 成员 2 支出 80 元
- 双方设备都应显示已使用 180 元
- 剩余预算为 1320 元

预算消耗应统计共享账本中双方所有符合条件的支出。

### 现有规则继续有效

- 退款按现有规则减少预算消耗
- 标记为"不计入统计"的账单不消耗预算
- 收入不应错误消耗支出预算
- 月份和分类规则沿用现有应用逻辑

### 预算同步

预算定义本身也必须同步，包括：
- 总预算
- 分类预算
- 月份、金额、币种、提醒阈值
- 新增、修改、删除

**预算冲突解决**：
- 使用 revision 机制
- revision 大的覆盖小的
- revision 相同时，使用 deviceId 决定胜负（确定性）
- 总预算和分类预算可以共存（不同实体）

---

## 十、本地资产规则

共享账本共享的是账单和预算，不共享资产账户。

每个人手机上的资产（现金、银行卡、支付宝、微信等）都属于本地私有数据。

### 10.1 自己创建的账单

当账单创建成员是当前用户时：
- 新建账单正常影响本地资产
- 修改金额时只调整差额
- 删除账单时恢复对应资产
- 重复收到同一操作不能重复扣款或恢复

### 10.2 对方创建的账单

当创建成员不是当前用户时：
- 账单仍显示在共享账本中
- 账单仍参与共享预算
- **不关联本机任何资产**
- **不改变本机资产余额**
- 即使本机存在同名账户，也不能自动匹配
- 不应出现在本机某个资产账户的流水中

### 10.3 本地资产绑定

创建人的设备可以保存：
> 共享账单身份 → 本地资产账户

这个绑定：
- 只保存在本机
- 不上传坚果云
- 不进入普通共享文件
- 不与另一名成员共享
- 换机后允许重新选择

---

## 十一、统计页面

### 现有统计页面（保持现状）

- 统计页面保持现状，只统计当前账本的内容
- 共享账本中，统计页面统计的是**所有共享账单**（包括双方的）
- 不需要按成员过滤

### 新增"双方各自用了多少钱"页面

在统计页面新增一个板块，显示：
- 成员1：支出 ¥1,200.00 / 收入 ¥500.00
- 成员2：支出 ¥800.00 / 收入 ¥300.00

**说明**：因为两个人的分类可能是不一样的，所以不进行跨成员的分类统计，只显示各自的总支出和总收入。

---

## 十二、同步状态和错误展示

### 同步状态显示

**位置**：首页账单页，搜索图标下方

**UI**：
```
┌─────────────────────────────────────────────────────────────┐
│  [账本切换]    2026-08    [日历]  [搜索]                     │
│                              [同步状态]  ← 点击可手动同步    │
│  ────────────────────────────────────────────────────────── │
│  本月收入    本月支出    本月结余                            │
│  ────────────────────────────────────────────────────────── │
│  同步失败：网络不可用，请检查网络  ← 错误信息                │
│  ────────────────────────────────────────────────────────── │
│  08-01  周五                                                │
│  [图标]  餐饮-外卖                                  -100.00 │
│                          [成员1]  现金                      │
└─────────────────────────────────────────────────────────────┘
```

**同步状态图标**：
- ✅ 同步成功：显示最近同步时间
- ⏳ 同步中：显示加载动画
- ❌ 同步失败：显示错误图标，点击查看详情
- 📤 待上传：显示待上传数量

**错误信息**：
- 横着显示在本月收入和本月结余那行下面
- 红色背景，简短描述
- 例如：网络不可用、账号或密码错误、同步请求过于频繁等

---

## 十三、离线与同步触发

没有网络时，用户仍然可以：
- 新增账单
- 修改自己的账单
- 删除自己的账单
- 修改预算

这些操作先保存在本地待同步队列中，恢复网络后自动重试。

### 同步触发时机

1. 打开应用
2. 进入或切换到共享账本
3. 新增、修改或删除共享账单后
4. 修改共享预算后
5. 用户手动下拉刷新
6. 后台定时任务
7. 网络恢复后的后台任务

多个触发同时发生时，同一个共享账本只能运行一个同步过程，避免重复请求和并发写入。

同步不能长期阻塞应用启动或账单编辑。

---

## 十四、坚果云同步行为

坚果云作为共享操作的交换空间，不作为整库备份恢复工具。

### 远端目录结构

```
/shared-ledger/{ledgerId}/
├── meta.json                    # 账本描述
├── device-{deviceA}/           # 设备A的操作目录
│   ├── 2026-08/
│   │   ├── op-uuid1.json
│   │   └── op-uuid2.json
│   └── 2026-09/
└── device-{deviceB}/           # 设备B的操作目录
    └── ...
```

### 操作文件格式

```json
{
  "operationId": "uuid",
  "type": "create|update|delete",
  "entityType": "bill|budget",
  "entityId": "uuid",
  "revision": 5,
  "deviceId": "device-uuid",
  "memberId": "member-uuid",
  "timestamp": 1693000000000,
  "payload": {
    // bill 或 budget 的具体数据
  }
}
```

### 同步过程

1. 读取远端目录，获取所有操作文件
2. 过滤已应用的操作（通过 operationId）
3. 按 revision 排序
4. 逐个应用操作（考虑冲突解决）
5. 上传本地待同步操作

### 文件清理

- 远端操作文件保留 30 天
- 30 天后可以清理
- 本地保留操作文件备份，需要时可以从本地恢复

---

## 十五、同步合并与冲突

系统不使用"云端文件覆盖本地数据库"的方式。

每次新增、修改或删除都会形成一条独立、不可变的操作。

每个共享实体保存自己的版本。

### 冲突解决规则

1. revision 较大的操作获胜
2. revision 相同时，deviceId 字典序大的获胜
3. 所有设备使用完全相同的比较规则

不使用手机系统时间决定胜负，因为两台手机时间可能不一致。

两人同时修改同一笔账单时，首版允许其中一方的修改被覆盖，但两台设备最终必须显示相同结果。

---

## 十六、幂等与故障恢复

系统必须正确处理以下情况：

### 重复同步

同一个操作可以被下载多次，但只能应用一次。

### 乱序同步

先收到新版、后收到旧版时，旧版不能覆盖新版。

### 删除保护

先收到删除、后收到旧修改时，账单不能复活。

### 上传中断

本地账单保存后、上传前应用退出：
- 账单仍存在
- 待上传操作仍存在
- 下次自动继续上传

### 上传结果丢失

远端上传成功、本机尚未标记成功时应用退出：
- 下次发现远端存在完全相同的操作文件，应视为上传成功
- 不能生成另一个 operationId
- 不能覆盖不同内容的同名文件

### 下载中断

操作下载后、应用到数据库前退出：
- 下次允许重新下载
- 不能因为半完成状态破坏本地账单

### 数据库事务中断

远端账单、版本状态、已应用记录和资产影响必须在一致的事务边界内处理。

不能出现：
- 账单已更新但版本状态没更新
- 资产已扣款但账单没保存
- 操作已标记完成但业务数据没应用

---

## 十七、备份与恢复

现有 `.bak` 备份继续用于灾难恢复，不参与共享同步。

### .bak 可以保留的信息

- 共享账单身份
- 所属共享账本身份
- 创建成员
- 关联退款账单身份

### .bak 不应包含的信息

- WebDAV 密码
- 当前设备身份相关秘密
- 待上传队列
- 已应用操作记录
- 本机同步错误
- 本地资产绑定
- 其他设备运行状态

恢复 `.bak` 后，用户需要重新连接共享账本。重新连接后通过云端全部操作重建同步运行状态。

普通个人账本的备份与恢复行为不能受到影响。

---

## 十八、安全说明

共享数据通过 HTTPS 传输，但首版保存在坚果云中的账单 JSON 默认是明文。

应用必须向用户说明：
- 获得 WebDAV 凭据的人可以读取或修改共享账本
- 该功能面向两个互相信任的人
- 首版不是严格权限系统
- 不应在公开渠道发送坚果云应用密码
- 删除本机共享账本不一定删除坚果云上的历史数据

首版不要求对共享文件进行 AES 加密，但未来可以增加。

---

## 十九、兼容性要求

增加共享功能后，现有功能不能被破坏：
- 普通个人账本
- 新增、修改、删除账单
- 退款
- 资产余额
- 预算统计
- 排除统计
- 分类
- 搜索
- 日历
- 回收站
- `.bak` 导出和恢复
- 现有坚果云云备份

数据库升级不能清空或丢失用户现有数据，不能通过 destructive migration 解决数据库版本问题。

---

## 二十、验收场景

### 基础共享

- 成员 1 创建共享账本，成员 2 成功加入
- 两边都能看到相同账本名称和成员
- 成员名称为空时显示"成员1""成员2"

### 双方记账

- 成员 1 新增账单，成员 2 同步后可见
- 成员 2 新增账单，成员 1 同步后可见
- 每笔账单显示正确创建成员
- 两边最终账单数量和内容相同

### 离线记账

- 两台手机均断网
- 双方分别新增多笔账单
- 本地立即保存成功
- 恢复网络后自动同步
- 双方最终看到所有账单，没有覆盖或丢失

### 编辑冲突

- 两人在离线状态下修改同一账单
- 恢复网络后按照固定冲突规则选出结果
- 两台设备最终显示一致
- 不依赖手机系统时间

### 删除

- 一方删除账单，另一方收到删除
- 旧修改随后到达时账单不复活
- 重复收到删除不会重复恢复资产

### 预算

- 两人分别记录同一分类支出
- 两端预算已用金额等于双方支出之和
- 退款和排除统计规则正确
- 双方修改同一预算后最终结果一致

### 私有资产

- 成员 1 的账单只影响成员 1 的本地资产
- 成员 2 的账单只影响成员 2 的本地资产
- 对方设备存在同名账户时也不自动关联
- 对方的账单不影响本机资产

### 重复和乱序

- 同一个操作重复下载多次，只应用一次
- 新版本先到、旧版本后到，旧版本不覆盖
- 删除先到、旧更新后到，账单不复活

### 应用退出和重启

- 保存后上传前退出，重启后继续上传
- 上传成功但本地未标记时退出，重启后不重复生成操作
- 同步过程中退出，不产生半应用数据

### WebDAV 异常

- 无网、超时、401、403、429、5xx 时本地账单不丢失
- 损坏 JSON 和未知版本文件不会破坏现有账单
- 凭据恢复后可以继续同步
- 超过单页目录上限时仍能同步全部操作

### 备份与升级

- 旧版本数据库升级后原账单、资产和预算不丢失
- 个人账单新增共享字段为空
- `.bak` 恢复不包含密码和同步运行队列
- 普通个人账本保持原有行为

---

## 二十一、完成标准

只有满足以下全部条件，才可以认为首版完成：

1. 两台真实 Android 设备可以创建并加入同一个共享账本
2. 两个人均可离线新增、修改、删除账单
3. 联网后双方账单最终一致
4. 同步不会整体覆盖任意一方本地数据库
5. 删除、重复和乱序操作得到正确处理
6. 共享预算统计双方符合规则的支出
7. 每个人只影响自己的本地资产
8. 应用退出、网络失败和重试不会丢失操作
9. WebDAV 分页和常见错误经过验证
10. 现有个人记账、资产、预算和备份没有回归
11. 数据库升级经过真实旧版本迁移测试
12. 两台真机完成主要场景测试
13. 凭据和敏感信息没有进入日志、数据库业务表、备份和共享文件

---

## 二十二、技术实现要点

### 数据库设计

**新增字段（Bill 表）**：
- `sharedId`: String? - 跨设备唯一ID
- `memberId`: String? - 创建成员ID
- `isShared`: Boolean - 是否是共享账单
- `cateIcon`: String? - 分类图标URL

**新增字段（Budget 表）**：
- `sharedId`: String? - 跨设备唯一ID
- `revision`: Long - 版本号
- `isShared`: Boolean - 是否是共享预算

**新增表**：
- `shared_ledger` - 共享账本
- `shared_member` - 共享成员
- `sync_operation` - 已应用的操作记录
- `sync_queue` - 待上传的操作队列
- `sync_state` - 同步状态

### 冲突解决

使用 Lamport Timestamp + deviceId 的组合：
- 每次操作 revision = max(本地revision, 收到的revision) + 1
- 冲突时：revision 大的获胜
- revision 相同时：deviceId 字典序大的获胜

### 设备标识

使用应用安装时生成的 UUID：
- 首次安装时生成，存储在 SharedPreferences
- 卸载重装会变化（符合预期：新设备需要重新加入）

### 同步锁

同一共享账本只能运行一个同步过程：
- 使用 Mutex 或 synchronized 实现
- 避免重复请求和并发写入

---

## 二十三、UI 设计要点

### 账单列表项

```
┌─────────────────────────────────────────────────────────────┐
│  [图标]  餐饮-外卖                                  -100.00 │
│                          [成员1]  现金                      │
└─────────────────────────────────────────────────────────────┘
```

- 成员名称显示在资产账户左侧
- 只有共享账单才显示成员名称
- 成员名称支持修改

### 同步状态

```
┌─────────────────────────────────────────────────────────────┐
│  [账本切换]    2026-08    [日历]  [搜索]                     │
│                              [同步状态]                     │
│  ────────────────────────────────────────────────────────── │
│  本月收入    本月支出    本月结余                            │
│  ────────────────────────────────────────────────────────── │
│  同步失败：网络不可用，请检查网络                           │
└─────────────────────────────────────────────────────────────┘
```

- 同步状态图标在搜索图标下方
- 错误信息在本月收入和本月结余那行下面
- 点击同步状态图标可手动触发同步

### 共享账本创建对话框

```
┌─────────────────────────────────────────┐
│  创建共享账本                           │
│  与另一人共同记账，通过坚果云同步       │
│                                         │
│  账本名称：[日常账本                ]   │
│                                         │
│  成员设置                               │
│  成员1名称：[                       ]   │
│  成员2名称：[                       ]   │
│                                         │
│  坚果云WebDAV配置                       │
│  WebDAV地址：[https://dav.jianguoyun...]│
│  坚果云用户名：[                    ]   │
│  坚果云应用密码：[                  ]   │
│  应用密码需在坚果云安全设置中生成       │
│                                         │
│                    [取消]  [创建]       │
└─────────────────────────────────────────┘
```

### 统计页面成员统计

```
┌─────────────────────────────────────────┐
│  统计页面                               │
│  ─────────────────────────────────────  │
│  [现有统计内容...]                      │
│  ─────────────────────────────────────  │
│  成员统计                               │
│  ┌─────────────────────────────────┐   │
│  │  成员1                          │   │
│  │  支出：¥1,200.00                │   │
│  │  收入：¥500.00                  │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │  成员2                          │   │
│  │  支出：¥800.00                  │   │
│  │  收入：¥300.00                  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 二十四、工作量估算

| 模块 | 工作量 | 说明 |
|------|--------|------|
| 数据库迁移 | 1天 | 添加字段和新表 |
| 共享账本创建/加入 | 1.5天 | UI和核心逻辑 |
| 账单同步 | 3天 | 核心同步逻辑 |
| 预算同步 | 1天 | 复用账单同步框架 |
| UI调整 | 1.5天 | 成员标签、同步状态、错误显示 |
| 成员统计页面 | 0.5天 | 新增板块 |
| 测试 | 1.5天 | 边开发边测试 |
| **总计** | **10天** | |

---

## 二十五、已完成功能

### 第一阶段：数据库和基础架构

✅ 数据库迁移（v34 → v35）
- 为 bills 表添加共享相关字段
- 为 budgets 表添加共享相关字段
- 创建新表：shared_ledger, shared_member, sync_operation, sync_queue, sync_state

✅ 数据模型
- 创建了所有新的实体类
- 创建了所有新的 DAO 接口
- 更新了现有实体

✅ UI 基础
- 修改了账单列表项布局，添加了成员名称显示
- 修改了账本管理页面，添加了"设定为共享账本"选项
- 在首页添加了同步状态图标和错误信息栏

✅ 工具类
- 创建了 DeviceIdManager 管理设备ID
- 创建了相关的图标和背景资源文件

### 待实现功能

⏳ 核心同步逻辑
- WebDAV 客户端
- 操作文件的上传和下载
- 冲突解决机制
- 资产处理逻辑

⏳ 共享账本创建/加入流程
- 创建共享账本的完整流程
- 加入共享账本的流程
- 首次同步的处理

⏳ 预算同步
- 预算的创建、修改、删除同步
- 预算冲突解决

⏳ 测试
- 单元测试
- 集成测试
- 真机测试
